function loggingOut(element) {
    element.innerText = "Logout";
}
function custom() {
    // we can include more code here if we want to
    console.log("this message is coming from js.");
}
function add_definition(element) {
    element.remove();
}
